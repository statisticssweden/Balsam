openapi: '3.0.2'
info:
  title: ChatProvider
  version: '2.0'
  description: This a service contract for the ChatProvider in Balsam.

servers:
  - url: http://chat-provider.balsam-system.svc.cluster.local/api/v1

tags:
  - name: Area
    description:  Topic methods

paths:
  /areas:
    post:
      tags: 
        - Area
      operationId: CreateArea
      description: Creates a new Area
      requestBody:
        description: Definition of a new area
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAreaRequest'    
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AreaCreatedResponse'
              
              example: 
                  name: demo_users
                  id: 12
        '400':
          $ref: '#/components/responses/ErrorResponse400'
  
  /areas/{areaId}:
    delete:
      tags: 
        - Area
      operationId: DeleteArea
      description: Deletes an Area
      parameters:
        - name: areaId
          in: path
          description: The id of the area
          required: true
          schema:
            type: string         
      responses:
        '200':
          description: Success
        '400':
          $ref: '#/components/responses/ErrorResponse400'
   
components:
  schemas:
    CreateAreaRequest:
      type: object
      required:
        - "name"
      properties:
        name:
          type: string
          description: The name of the area

    AreaCreatedResponse:
      type: object
      description: Group created response
      required:
        - "id"
        - "name"
      properties:
        name:
          type: string
          description: The name of the area
        id:
          type: string
          description: The id to the area that can be used for accessing it.

    Problem:
      type: object
      properties:
        type:
          type: string
          format: uri
          description: |
            An absolute URI that identifies the problem type.  When dereferenced,
            it SHOULD provide human-readable documentation for the problem type
            (e.g., using HTML).
          default: about:blank
          example: https://zalando.github.io/problem/constraint-violation
        title:
          type: string
          description: |
            A short, summary of the problem type. Written in english and readable
            for engineers (usually not suited for non technical stakeholders and
            not localized); example: Service Unavailable
        status:
          type: integer
          format: int32
          description: |
            The HTTP status code generated by the origin server for this occurrence
            of the problem.
          minimum: 100
          maximum: 600
          exclusiveMaximum: true
          example: 503
        detail:
          type: string
          description: |
            A human readable explanation specific to this occurrence of the
            problem.
          example: Connection to database timed out
        instance:
          type: string
          format: uri
          description: |
            An absolute URI that identifies the specific occurrence of the problem.
            It may or may not yield further information if dereferenced.
                  
  responses:
    ErrorResponse400:
      description: Error respsone for 400
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          example:
            title: Bad Request
            status: 400
            detail: Generic error
 